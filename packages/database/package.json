{
  "name": "database",
  "main": "./dist/index.js",
  "types": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "tsconfig": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^4.13.0",
    "typescript": "^5.0.4"
  }
}
