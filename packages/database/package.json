{
  "name": "database",
  "main": "./dist/index.js",
  "version": "0.2.1",
  "types": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "5.1.1",
    "tsconfig": "*"
  },
  "devDependencies": {
    "prisma": "5.1.1",
    "typescript": "^5.0.4"
  }
}
